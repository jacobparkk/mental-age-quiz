import{useMemo,useState}from'react';import{QUESTIONS}from'../constants';import{Option,Result,Trait}from'../types';
export function calculateResult(answers:Option[]):Result{const weights=answers.map((_,i)=>1+i*.035),sum=weights.reduce((a,b)=>a+b,0),age=Math.round(answers.reduce((n,a,i)=>n+a.age*weights[i],0)/sum);const raw={curiosity:0,stability:0,social:0,reflection:0};answers.forEach(a=>(Object.keys(a.traits)as Trait[]).forEach(k=>raw[k]+=a.traits[k]||0));const max=Math.max(...Object.values(raw),1),traits=Object.fromEntries(Object.entries(raw).map(([k,v])=>[k,Math.round(v/max*100)]))as Record<Trait,number>;const band=age<23?['The Bright Spark','Your mind runs on possibility, momentum, and first-hand discovery.','#ff7a90']:age<33?['The Curious Connector','You mix fresh energy with a growing instinct for what actually matters.','#ffad5c']:age<43?['The Intentional Improviser','You balance spontaneity with self-knowledge and increasingly selective energy.','#66d9c0']:age<55?['The Grounded Explorer','Experience has sharpened your judgment without closing your curiosity.','#7aa7ff']:['The Old-Soul Navigator','You value depth, perspective, and the kind of confidence that does not need an audience.','#b99cff'];return{age,title:band[0],subtitle:band[1],color:band[2],traits,answers}}
export const useQuizEngine=()=>{const[index,setIndex]=useState(-1),[answers,setAnswers]=useState<Option[]>([]),[dark,setDark]=useState(()=>localStorage.getItem('mindprint-theme')!=='light');const result=useMemo(()=>answers.length===QUESTIONS.length?calculateResult(answers):null,[answers]);const answer=(a:Option)=>{const next=answers.slice(0,index);next[index]=a;setAnswers(next);setIndex(index+1)};const back=()=>index>0&&setIndex(index-1);const restart=()=>{setAnswers([]);setIndex(-1)};const toggleTheme=()=>setDark(v=>{localStorage.setItem('mindprint-theme',v?'light':'dark');return!v});return{index,answers,result,dark,start:()=>setIndex(0),answer,back,restart,toggleTheme}}
